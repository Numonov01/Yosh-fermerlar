import { teamtab, basicTeamData } from "./teampage";

// faq
import { faqdata, faqitems, privacy } from "./faq"

// Api
import { apiData } from "./apikey";

// Contact
import { contactData } from "./contact";

// Todo
import { todoList, todoimg, insights, project } from "./todo";

// Email 
import { mailbox } from "./email";

// Chat
import { direactContact, channelsList, messages, attachements, chatContactData } from "./chat"

// Kanban
import { headData, tasklist, AddTeamMember } from "./kanban"

// Dashboard
import { recentOrders, chatList, chatHistory, chatmsg, BestSellingProductsData, stockReportData, allRevenueChartData, monthRevenueChartData, halfYearRevenueChartData, yearRevenueChartData } from "./dashboard"

export {
    teamtab,
    basicTeamData,
    faqdata, faqitems, privacy,
    apiData,
    contactData,
    todoList, todoimg, insights, project,
    mailbox,
    direactContact, channelsList, messages, attachements, chatContactData,
    headData, tasklist, AddTeamMember,
    recentOrders, chatList, chatHistory, chatmsg, BestSellingProductsData, stockReportData, allRevenueChartData, monthRevenueChartData, halfYearRevenueChartData, yearRevenueChartData
}